spring:
  application:
    name: myWebPage
  datasource:
    url: jdbc:mysql://localhost:3306/webpage?serverTimezone=Asia/Seoul
    username: root
    password: 1234

server:
  port: 8080
  servlet:
    context-path: /
    encoding:
      charset: UTF-8
      enabled: true
      force: true

